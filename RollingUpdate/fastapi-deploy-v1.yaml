apiVersion: apps/v1
kind: Deployment
metadata:
  name: fastapi-deploy
spec:
  replicas: 3
  selector:
    matchLabels:
      app: fastapi
  template:
    metadata:
      labels:
        app: fastapi
    spec:
      containers:
        - name: fastapi
          image: YOUR_DOCKERHUB_USER/docker-fastapi:v1
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
